# Power Apps Code Apps
My repository for exploring code apps and best practices for building and deploying them.

### Step 1: Create React App
```
npm create vite@latest my-app -- --template react-ts
```

(or build in current dirctory via `npm create vite@latest . -- --template react-ts`)